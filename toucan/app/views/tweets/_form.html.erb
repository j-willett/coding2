<%= form_with(model: tweet, local: true, class: "navbar-form navbar-left") do |form| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.text_field :message, id: :tweet_message, placeholder: "What's happening?", class: "form-control" %>
    <%= form.hidden_field :user_id, id: :tweet_user_id, value: current_user.id %>
    <%= form.submit "Tweet it!", class: "btn btn-default" %>
  </div>
<% end %>
